{
  "version": "0.2.0",
  "configurations": [{
    "name": "运行图像观察器",
    "type": "extensionHost",
    "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/fixtures"]
  }]
}
